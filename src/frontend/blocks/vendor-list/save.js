/**
 * Save component for the Vendor List block
 *
 * This component returns null as the block is rendered on the server with PHP
 *
 * @return {null} Return null for server-side rendered blocks
 */
const VendorListSave = () => {
	return null;
};

export default VendorListSave;
