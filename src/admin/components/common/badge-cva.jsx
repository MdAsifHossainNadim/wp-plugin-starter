// This file has been consolidated into badge.jsx
// It is kept for backward compatibility but will be removed in a future version

/**
 * WordPress dependencies
 */
import { Badge, badgeStyles } from './badge';

// Re-export Badge and badgeStyles for backward compatibility
export { Badge as BadgeCVA, badgeStyles };
